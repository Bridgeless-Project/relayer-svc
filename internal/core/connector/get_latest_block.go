package connector

import (
	"context"

	bridgeTypes "github.com/Bridgeless-Project/bridgeless-core/v12/x/bridge/types"
)

func (c *Connector) GetLatestBlock(ctx context.Context) (int64, error) {
	msg := c
}
